# x87 `x86_fp80` 设计说明

## 设计理念

本项目中的 x87 支持首先服务于**函数签名恢复和类型推理**，不是实现一个完整的 x87 指令级模拟器。

总体原则是：在保留签名恢复和类型推理所需信息的前提下，尽量精简需要暴露的状态，以及由 x87 特殊语义带来的转换复杂度。由此，当前实现中的若干近似不是暂时遗漏，而是与目标一致的取舍：

- 只暴露会影响签名和类型判断的 x87 值；
- 对不影响类型判断的栈状态、环境状态和异常状态保持最小建模；
- 对尚未在目标样本中遇到的指令不提前猜测通用语义；
- 无法完全确定时，根据已有的函数体、调用点、寄存器读写和外部原型信息做最佳推断，并保留不确定性边界。

这里的 `x86_fp80` 表示 x86 ELF SysV ABI 下的 80 位 extended precision 值，对应 LLVM 的 `x86_fp80` 类型。本文不把它扩展解释为 IEEE binary128 或其他平台的 `long double`。

## 当前的表示方式

### ST0/ST1：显式窗口

`ST0` 和 `ST1` 建成真实的 LLVM 寄存器全局，底层保存 80 位 payload；窗口内的算术和栈操作可以参与普通的 register summary/SSA 流程。

当前窗口模型覆盖的主要操作包括：

- `FLD`、`FILD`、`FLDZ`、`FLD1`；
- `FST`、`FSTP`、`FISTP`；
- `FXCH`；
- `FADD`、`FSUB`、`FMUL`、`FDIV` 及 pop/reverse 形式；
- `FSQRT`、`FABS`、`FCHS`；
- `FCOMI`、`FCOMIP`、`FUCOMI`、`FUCOMIP`。

窗口边界使用 LLVM 浮点类型参与计算，寄存器 summary 仍能看到 `ST0` 的真实读写。因此，函数尾写入 `ST0`、调用方在调用后读取 `ST0` 的场景，可以按普通返回寄存器的需求传播恢复为 `x86_fp80` 返回值。

### ST2..ST7：opaque runtime 状态

`ST2..ST7` 不建立 LLVM 全局，也不进入静态寄存器 SSA。它们由外部声明的 `notdec.x87.*` 函数维护，当前只通过以下边界操作衔接窗口：

```text
notdec.x87.push(x86_fp80)
notdec.x87.pop() -> x86_fp80
notdec.x87.peek(i8) -> x86_fp80
notdec.x87.poke(i8, x86_fp80)
```

这是精心设计的选择，而不是缺少一个完整 x87 栈模型：

- `ST0`/`ST1` 已经足够支撑当前签名恢复和类型推理；
- 暴露 `ST2..ST7` 会把 TOP、栈深、tag word、空槽以及跨调用栈状态引入静态分析，显著增加复杂度；
- 对当前目标，深层栈状态的主要作用是保存和搬移 `x86_fp80` 值，而不是决定函数参数/返回类型。

因此，当前不追踪 x87 逻辑栈深，也不把 `ST2..ST7` 当作可直接恢复的独立函数参数或返回值。

## 指令处理边界

### 已处理的指令类别

除窗口操作外，当前还为类型推理保留了以下外部声明边界：

- `FPREM`：声明为接受两个 `x86_fp80`、返回一个 `x86_fp80` 的操作；
- `FNSTSW`、`FNSTCW`、`FLDCW`：以状态字/控制字的整数类型传递；
- `FSTENV`、`FLDENV`：以内存指针传递环境快照。

这些声明的重点是让类型流和调用边界可表达，不要求 lifting 在本项目内提供一个可执行的 x87 runtime。

### 未识别的 native x87 指令

未被助记符分类器识别的 native x87 指令会保留 warning，并等待后续遇到具体使用该指令的样例后再处理。当前不针对尚未出现的指令族预先设计一套推测语义，也不为了消除 warning 而引入可能错误的泛化转换。

例如，`FCOM/FCOMP/FCOMPP`、`FTST`、`FXAM`、`FPREM1`、`FSIN`、`FCOS`、`FPTAN`、`FPATAN`、`FYL2X`、`FSCALE`、`FXTRACT`、BCD 转换和部分栈管理指令，均属于按实例驱动的后续工作范围。

处理顺序是：先确认真实指令的 p-code、操作数和类型影响，再决定它应当进入窗口模型、转换为声明边界，还是保持更保守的 fallback。

## `FISTP` 与舍入语义

当前 `FISTP` 的目标是表达以下类型关系：

```text
x86_fp80 输入 -> 整数输出，并执行一次 x87 pop
```

因此，当前 lifting 可以用 LLVM 的浮点到整数转换表示其类型效果，但**不承诺完整的 x87 数值语义**。暂不修改的部分包括：

- control word 中 rounding-control 对 `FISTP` 的影响；
- precision-control、异常标志和 integer indefinite 结果；
- 溢出、NaN、无穷和非规格化数的精确硬件行为。

`FLDCW`/`FNSTCW` 仍然保留控制字的类型边界，使后续模型可以识别控制字的输入和输出；但在当前签名恢复和类型推理阶段，不要求它驱动 LLVM 数值转换。

同理，浮点环境和异常状态对函数签名及基本类型推断的影响有限，因此当前只做足以表达类型边界的近似，不把它们作为本阶段的重点。

## 内部函数的 ST0 返回恢复

内部函数的 `long double` 返回不依赖“`ret` 前最后一条指令是什么”的专门模式匹配，而是尽量复用 summary/SSA 的需求传播：

1. lifting 把 `ST0` 的写入保留为寄存器全局写入；
2. 调用方在调用后真实读取 `ST0` 时，产生对 callee 输出的需求；
3. callee 同时确实改写 `ST0` 时，返回槽可以恢复为 `x86_fp80`；
4. SysV x86 下 `ST0` 不作为普通入口参数加入内部函数签名。

这个推断不可能覆盖所有控制流、间接调用和外部消费方式。当前策略是根据已有信息做到最好：

- 有明确 caller 读取证据时恢复 `x86_fp80` 返回；
- 没有足够证据时不强行扩大函数签名；
- 不通过无条件 seed `ST0` 让需求沿整个调用图扩散，以免制造大量错误返回槽。

因此，内部 ST0 返回恢复是基于证据的最佳努力推断，而不是对所有二进制都绝对完备的 ABI 证明。

## 外部、未知和间接调用

外部调用分为两类，签名信息来源不同。

### 有函数体的函数

只要函数体可见，就优先根据函数体和使用点恢复签名：

- 参数由入口读取、寄存器写入和调用点传值关系推断；
- 返回由函数内部改写的寄存器、调用方实际读取的结果和需求传播推断；
- `ST0` 是否为 `x86_fp80` 返回也必须有相应的使用证据。

这条规则适用于名称为 `powl`、`sinl` 等但本身已经被 lifting 出函数体的函数；函数名不是签名依据。

### 没有函数体的导入函数

没有函数体时，函数名是可用的外部信息。此时使用预先提供的可信函数签名：

- 默认原型表提供已确认的常用原型，例如 `sqrtl(long double)`；
- 用户可以通过 JSON prototype overlay 提供额外函数签名；
- `long double` 参数按 x86 SysV 栈参数处理，返回按 `ST0`/`x86_fp80` 处理；
- 不因为名字带有 `l` 后缀就批量猜测其签名。

因此，对于没有函数体的 `powl`、`sinl`、`frexpl` 等函数，正确做法是补充可信原型，而不是从未知调用的寄存器痕迹中强行推导完整 ABI。

### 完全未知的外部或间接调用

完全未知的函数不保证可靠恢复 `long double` 返回。当前只根据已有信息做最佳努力推断，例如：

- 旧 LLVM 返回类型；
- 调用后被实际读取的 ABI 输出寄存器；
- 是否存在明确的 `ST0` 读取证据；
- 已知的参数数量和寄存器/栈位置。

如果证据不足，保持保守结果并报告相应 warning，比凭空把调用声明成 `x86_fp80` 更符合类型推理目标。间接调用也遵循同一原则，不承诺在缺少目标原型时恢复完整的 long double ABI。

## Runtime 声明的定位

当前仓库只需要声明 `notdec.x87.*` 函数，不需要在 lifting 阶段实现它们的运行时执行逻辑。原因是本阶段的产物服务于签名恢复和类型推理，runtime 函数的关键作用是提供明确的类型边界和状态影响边界：

- `push/pop/peek/poke` 表示深层 x87 状态的 opaque 读写；
- `fprem` 表示 `x86_fp80` 到 `x86_fp80` 的计算边界；
- `fnstsw/fnstcw/fldcw/fstenv/fldenv` 表示环境数据的整数或内存边界；
- SummarySSA 将这些函数视为 x87 专用 helper，不把它们误当作普通 ABI 外部调用。

后续如果出现需要执行级验证的场景，可以另外提供匹配这些声明的 runtime；这不改变当前 lifting 的类型推理职责。

## 后续规划：复杂 long double ABI

以下内容暂不属于当前阶段，留作后续按真实样例推进的规划：

- `complex long double` 的 ST0+ST1 双值返回；
- 包含多个 `long double` 的结构体、数组和聚合返回；
- sret 以及聚合参数的完整 ABI 分类；
- vararg 中的 long double 传递和恢复；
- 其他平台或 ABI 的 `long double`，包括 IEEE 128-bit 和非 x86 ELF 规则；
- 需要精确 control word、异常状态、tag word 或实际数值结果的执行级验证。

这些规划项只有在出现对应的真实函数体、导入原型或 Bench2 用例后才进入实现。届时仍应遵守本文的基本原则：先满足签名恢复和类型推理，再评估是否有必要暴露更多 x87 状态或增加更复杂的转换模型。

## 当前判断标准

当前实现满足以下条件即可视为达到本阶段目标：

- `ST0` 的可见读写能够参与 SummarySSA，并在有证据时恢复为 `x86_fp80` 返回；
- 已知外部原型能够把 `long double` 参数和 `ST0` 返回表达为正确 LLVM 类型；
- `ST2..ST7` 不泄漏为普通寄存器参数或返回值；
- 未识别指令明确报告并等待具体样例，而不是静默伪造语义；
- x87 helper 声明不破坏 LLVM IR 的类型一致性和 verifier；
- 在不要求执行级数值精度的前提下，保留足够信息支持后续签名恢复和类型推理。
